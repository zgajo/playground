const path = require("path");

module.exports = {
  config: path.resolve(path.join(__dirname, "sequelize-cli-config.json")),
  "models-path": path.resolve(path.join(__dirname, "dist/src/db/models")),
  "seeders-path": path.resolve(path.join(__dirname, "dist/src/db/models")),
  "migrations-path": path.resolve(
    path.join(__dirname, "dist/src/db/migrations")
  ),
};
